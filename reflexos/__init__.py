"""ReflexOS — an AI-native training layer for robot workers.

The robot's body is exposed as an MCP server (``reflexos.server``); a reasoning
agent operates it through a pluggable cognitive backend that attempts workflows,
evaluates outcomes, corrects failures, and saves successful routines as reusable
robot reflexes.

See ``PLAN.md`` for the full architecture and build phases.
"""

__version__ = "0.1.0"
