{
  "name": "ee_y_minus_1cm",
  "description": "Translate the gripper about 1 cm in URDF -Y from the current pose.",
  "notes": "Reusable relative free-space nudge; verify with get_ee_pose.",
  "steps": [
    {
      "tool": "move_ee_by",
      "args": {
        "dy": -0.01,
        "max_step_m": 0.01,
        "timeout": 2.0,
        "tolerance": 3.0
      }
    }
  ]
}